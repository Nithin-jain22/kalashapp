/* Generated placeholder SVG icons for PWA */

/* Icon Utility: Generate SVG data URLs for PWG icons */
export function generateSvgIcon(size = 192, label = "T") {
  return `data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 ${size} ${size}'><rect fill='%232f73ff' width='${size}' height='${size}'/><circle cx='${size / 4}' cy='${size / 4}' r='${size / 6}' fill='%23ff6b6b'/><rect x='${size / 3}' y='${size / 2.5}' width='${size / 3}' height='${size / 3}' fill='%2351cf66'/><polygon points='${size * 0.75},${size * 0.25} ${size * 0.9},${size * 0.75} ${size * 0.6},${size * 0.75}' fill='%23ffd43b'/></svg>`;
}
